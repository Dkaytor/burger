<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
		<script src="https://code.jquery.com/jquery.js"></script>
		 <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
		<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
		<link rel="stylesheet" href="/assets/css/style.css" type="text/css" />
		<script src="/assets/js/burgers.js"></script>
		
	</head>


<h1>Eat Da Burger!</h1>

<img src="../assets/img/burger.jpg" alt="burger" class="center">

<div class="row">
	<div class="col-md-4">
		<h3>Devour the burger of your choice!</h3>

		<ul>
			{{#each burger}}
			{{#unless devour}}
			<h4>{{this.burger_name}}
				<button type="submit" class="change-devour" data-id={{id}}>
					{{!-- data-newdevour={{devour}} --}}
				DEVOUR!
				</button>
				</h4>
				{{/unless}}
			{{/each}}
		</ul>
		</div>
		<div class="col-md-2"></div>

		<div class="col-md-6">
			<h3>Burgers Devoured!</h3>
			<ul>
				{{#each burger}}
				{{#if devour}}
				<h4>{{this.burger_name}}</h4>
				{{/if}}
				{{/each}}
			</ul>

		</div>
</div>
<div class="text-center">
<form class="create-form">

	<h4><label for="bg">Burger Name:</label></h4>
	<input type="text" id="bg" name="burger_name">
	<button class="submit" align="right">Submit</button>
</form>
</div>